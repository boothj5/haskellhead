To build:

    cabal configure
    cabal build

To run:

    ./dist/build/haskellhead/haskellhead

I to just install straight to your ~/bin directory

    cabal install --prefix=$HOME --user

And provided your bin directory is on your PATH, to run

    haskellhead

Running tests

    cd testsuite
    ./runtests.sh
